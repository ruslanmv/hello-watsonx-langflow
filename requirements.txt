# LangFlow + watsonx.ai Demo Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
langflow>=1.0.0
langchain-ibm>=0.1.0
python-dotenv>=1.0.0

# Optional: For development
ipython>=8.0.0
jupyter>=1.0.0

# LangChain core (usually installed with langchain-ibm)
langchain>=0.1.0
langchain-core>=0.1.0

# IBM watsonx SDK (usually installed with langchain-ibm)
ibm-watsonx-ai>=1.1.0

# Note: Some dependencies are automatically installed with the main packages
